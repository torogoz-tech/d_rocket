# Generated by build_runner — committed when stable, ignored while
# running locally. The `d_rocket/example/end_to_end/entities.g.dart`
# is the only hand-checked-in generated file and is required for the
# demo to be runnable without `dart run build_runner build`.
*.g.dart
!lib/example/end_to_end/entities.g.dart

# Dart / Pub
.dart_tool/
.packages
build/
pubspec.lock
pubspec_overrides.yaml

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.iml
rename_rocket.py
commit_msg_rename.txt

# Serena cache (per-project MCP state)
.serena/
