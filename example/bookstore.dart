// MOVED: this file is now at `lib/example/bookstore.dart`.
// It was moved during the "absorb d_rocket_sqlite"
// migration. The codegen for the bookstore's `Author` /
// `Book` / `Sale` `extends Record` classes lives next to the
// entities (in `lib/`) so the `d_rocket_builder:record`
// builder can see them as part of the regular source tree.
//
// This stub is kept at the original path for
// backward-compat with the README's
// `dart run example/bookstore.dart` instructions; it simply
// re-exports the new location.
export 'package:d_rocket/example/bookstore.dart';
